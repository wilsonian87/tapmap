# TapMap Configuration
SECRET_KEY=change-me-to-a-random-string
DATABASE_PATH=data/tapmap.db

# Crawl defaults (can also be set per-scan)
MAX_PAGES_DEFAULT=200
MAX_DEPTH_DEFAULT=5
RATE_LIMIT_DEFAULT=1.0
SCAN_TIMEOUT_SECONDS=900
